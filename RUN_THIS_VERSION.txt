VERSION 10 — AI WEEK SUMMARY

Run:
    pip install -r requirements.txt
    streamlit run app.py

You should see:
    Version 10 — AI week summary and schedule reasoning

After a weekly schedule is created, the app now displays:
    AI summary of the week

This explains:
- the clustering strategy
- why sites were deferred
- tighter/looser days
- weather/TfL considerations
- lower-confidence duration predictions

The summary is explanation-only and cannot alter the validated schedule.
