VERSION 15 — UNIFIED WEEKLY SCHEDULER + SAME-CAMPUS BYPASS

Run:
    pip install -r requirements.txt
    streamlit run app.py

Look for:
    Version 15 — unified weekly scheduling + same-campus routing bypass

Key cost optimisation:
    same full postcode + similar building/address name
    = same campus

Google Route Matrix sees only ONE representative destination per campus.
Movement between buildings on that campus uses the fixed Same-campus transfer
time (default 5 minutes) and does not call Google.

Google is still used:
    - to reach the campus
    - between different campuses
    - to validate the trip home

Hard horizon remains:
    Google Routes is used ONLY for the single selected week.
