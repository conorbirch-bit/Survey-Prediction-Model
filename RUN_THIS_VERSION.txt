VERSION 20.12.7 — REMOVE FINAL FILLING RATIO ONLY

Apply over version 20.12.6 and restart Streamlit.
Run: python -m streamlit run app.py

The final filling pass can add a survey even when the extra travel takes longer
than the survey. The existing 15 km wider-search radius remains. Initial route
planning retains its normal efficiency preference; only final filling drops the
minimum survey-to-travel ratio.

The original gap-priority order and empty-day area shortlist are retained.
Access rules, completed-work exclusions, existing bookings, lunch, survey finish
and return-home deadlines remain enforced. The seven approved resolved issues
and standalone access report are unchanged.

Included: app.py, team_scheduler.py, RUN_THIS_VERSION.txt
and tests/test_fuller_days.py. Replace matching files and keep everything else.
89 offline regression tests passed. Rerun with your live Google connection for results.
